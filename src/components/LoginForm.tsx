'use client'
import {useState} from 'react'
import {supabase} from '@/lib/supabase'
export default function LoginForm(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function login(e:React.FormEvent){e.preventDefault();setMsg('Entrando...');const {data,error}=await supabase.auth.signInWithPassword({email,password});if(error){setMsg(error.message);return}const {data:profile,error:pe}=await supabase.from('profiles').select('role,active,franchise_id').eq('id',data.user.id).single();if(pe||!profile?.active){await supabase.auth.signOut();setMsg('Conta inativa ou sem perfil administrativo.');return}if(profile.role==='super_admin'){window.location.href='/dashboard';return}if(profile.role==='franchise_admin'&&profile.franchise_id){window.location.href='/franqueado';return}await supabase.auth.signOut();setMsg('Este usuário não possui acesso administrativo.')}return <div className="card" style={{maxWidth:460,margin:'10vh auto'}}><div className="eyebrow">CLICK-GO</div><h1 className="title">Acesso Administrativo</h1><p className="subtitle">A matriz e os franqueados usam o mesmo login; o sistema identifica automaticamente o painel correto.</p><form onSubmit={login} className="module-list" style={{marginTop:22}}><div className="field"><label>E-mail</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Senha</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="button">Entrar</button>{msg&&<p className="empty">{msg}</p>}</form></div>}
