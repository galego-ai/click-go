'use client'

import { useEffect, useRef, useState } from 'react'
import { supabase } from '@/lib/supabase'
import AddressSearch from '@/components/AddressSearch'
import PassengerDriverTracker from '@/components/PassengerDriverTracker'
import 'leaflet/dist/leaflet.css'

type Point={lat:number;lng:number}
type PaymentMethod={id:string;method_type:string;brand:string|null;last4:string|null;is_default:boolean}
type RideOption={city_id:string;franchise_id:string;city_name:string;state:string;category_id:string|null;category_name:string|null;required_vehicle_type:string|null;distance_km:number|string;duration_min:number|string;fare:number|string|null;multiplier:number|string|null}
type PaySettings={cash_enabled:boolean;pix_enabled:boolean;card_app_enabled:boolean;card_machine_enabled:boolean;card_credit_enabled:boolean;card_debit_enabled:boolean;app_card_gateway:string|null;card_surcharge_percentage:number|string;card_fee_bearer:string;installments_enabled:boolean;max_installments:number}
type Ride={id:string;status:string;driver_id:string|null;estimated_fare:number|string|null;final_fare:number|string|null;origin_label:string;destination_label:string;estimated_arrival_min:number|null;cancellation_fee_applied:boolean;cancellation_fee_amount:number|string;driver_departed_at:string|null;cancellation_free_seconds:number;cancellation_policy_fee:number|string;carryover_charge_amount:number|string}
type SearchResult={label:string;lat:number;lng:number}
type CancelPrompt={fee:number;remainingFreeSeconds:number;requiresFee:boolean}

const card:React.CSSProperties={background:'#fff',border:'1px solid #ececec',borderRadius:22,padding:16,color:'#111',boxShadow:'0 6px 24px rgba(0,0,0,.06)'}
const yellowBtn:React.CSSProperties={background:'#ffd400',color:'#000',border:0,borderRadius:16,padding:'16px 18px',fontWeight:900,fontSize:17,cursor:'pointer',width:'100%'}
const money=(v:any)=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})
const rideSelect='id,status,driver_id,estimated_fare,final_fare,origin_label,destination_label,estimated_arrival_min,cancellation_fee_applied,cancellation_fee_amount,driver_departed_at,cancellation_free_seconds,cancellation_policy_fee,carryover_charge_amount'

function vehicleIcon(type:string|null){const t=(type||'').toLowerCase();if(t.includes('moto'))return '🏍️';if(t.includes('premium')||t.includes('comfort'))return '🚘';if(t.includes('taxi')||t.includes('táxi'))return '🚕';return '🚗'}
function paymentLabel(v:string){return v==='pix'?'PIX':v==='card'?'Cartão no app':v==='card_machine'?'Cartão com motorista':'Dinheiro'}
function statusLabel(v:string){return v==='requested'||v==='searching'?'Procurando motorista':v==='accepted'?'Motorista a caminho':v==='driver_arriving'?'Motorista se deslocando':v==='in_progress'?'Em corrida':v==='completed'?'Corrida concluída':v==='cancelled'?'Corrida cancelada':v}

export default function PassengerRideRequest({methods,onRideCreated}:{methods:PaymentMethod[];onRideCreated?:()=>void}){
 const mapEl=useRef<HTMLDivElement|null>(null),mapRef=useRef<any>(null),layerRef=useRef<any>(null),autoLocated=useRef(false),calcSeq=useRef(0)
 const[origin,setOrigin]=useState<Point|null>(null),[destination,setDestination]=useState<Point|null>(null)
 const[originLabel,setOriginLabel]=useState('Obtendo sua localização...'),[destinationLabel,setDestinationLabel]=useState('')
 const[editingOrigin,setEditingOrigin]=useState(false),[step,setStep]=useState<'search'|'options'>('search')
 const[options,setOptions]=useState<RideOption[]>([]),[selected,setSelected]=useState(''),[payment,setPayment]=useState('cash'),[paySettings,setPaySettings]=useState<PaySettings|null>(null)
 const[busy,setBusy]=useState(false),[msg,setMsg]=useState(''),[currentRide,setCurrentRide]=useState<Ride|null>(null),[now,setNow]=useState(Date.now()),[cancelPrompt,setCancelPrompt]=useState<CancelPrompt|null>(null)

 useEffect(()=>{if(autoLocated.current)return;autoLocated.current=true;useMyLocation(true)},[])
 useEffect(()=>{if(currentRide?.status!=='driver_arriving')return;const t=window.setInterval(()=>setNow(Date.now()),1000);return()=>window.clearInterval(t)},[currentRide?.status])
 useEffect(()=>{const preferred=methods.find(m=>m.is_default)||methods[0];if(preferred?.method_type==='card'&&paySettings?.card_app_enabled)setPayment('card')},[methods,paySettings?.card_app_enabled])
 useEffect(()=>{if(origin&&destination&&!currentRide)void calculate()},[origin?.lat,origin?.lng,destination?.lat,destination?.lng])

 useEffect(()=>{let alive=true;(async()=>{if(!mapEl.current)return;const L=await import('leaflet');if(!alive)return;if(!mapRef.current){mapRef.current=L.map(mapEl.current,{zoomControl:false}).setView([-14.52472,-49.14083],13);L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'&copy; OpenStreetMap contributors',maxZoom:19}).addTo(mapRef.current);layerRef.current=L.layerGroup().addTo(mapRef.current);L.control.zoom({position:'bottomright'}).addTo(mapRef.current)}layerRef.current.clearLayers();const pts:[number,number][]=[];if(origin){L.circleMarker([origin.lat,origin.lng],{radius:9,weight:4,color:'#111',fillColor:'#19c7a3',fillOpacity:1}).bindTooltip('Embarque').addTo(layerRef.current);pts.push([origin.lat,origin.lng])}if(destination){L.circleMarker([destination.lat,destination.lng],{radius:9,weight:4,color:'#111',fillColor:'#ff8a3d',fillOpacity:1}).bindTooltip('Destino').addTo(layerRef.current);pts.push([destination.lat,destination.lng])}if(origin&&destination)L.polyline([[origin.lat,origin.lng],[destination.lat,destination.lng]],{weight:7,opacity:.85}).addTo(layerRef.current);if(pts.length>1)mapRef.current.fitBounds(pts,{padding:[45,45]});else if(pts.length===1)mapRef.current.setView(pts[0],16)})();return()=>{alive=false}},[origin,destination,step,currentRide?.id])
 useEffect(()=>()=>{if(mapRef.current){mapRef.current.remove();mapRef.current=null}},[])
 useEffect(()=>{if(!currentRide?.id)return;const ch=supabase.channel(`passenger-ride-${currentRide.id}`).on('postgres_changes',{event:'UPDATE',schema:'public',table:'rides',filter:`id=eq.${currentRide.id}`},(payload:any)=>{setCurrentRide(payload.new as Ride);if(['completed','cancelled'].includes(payload.new.status)){setCancelPrompt(null);onRideCreated?.()}}).subscribe();return()=>{supabase.removeChannel(ch)}},[currentRide?.id,onRideCreated])

 function clearCalculated(){setOptions([]);setSelected('');setPaySettings(null)}
 function useMyLocation(silent=false){if(!navigator.geolocation){if(!silent)setMsg('GPS indisponível. Pesquise outro endereço para a origem.');setOriginLabel('Escolha um endereço de origem');return}if(!silent)setBusy(true);navigator.geolocation.getCurrentPosition(pos=>{const p={lat:pos.coords.latitude,lng:pos.coords.longitude};setOrigin(p);setOriginLabel('Minha localização atual');setEditingOrigin(false);clearCalculated();setBusy(false)},()=>{setOriginLabel('Escolha um endereço de origem');setEditingOrigin(true);if(!silent)setMsg('Não foi possível obter sua localização. Digite o endereço de origem.');setBusy(false)},{enableHighAccuracy:true,timeout:12000,maximumAge:15000})}
 function chooseOrigin(r:SearchResult){setOrigin({lat:r.lat,lng:r.lng});setOriginLabel(r.label);setEditingOrigin(false);clearCalculated();setMsg('')}
 function chooseDestination(r:SearchResult){setDestination({lat:r.lat,lng:r.lng});setDestinationLabel(r.label);setStep('options');clearCalculated();setMsg('Buscando as categorias disponíveis...')}
 async function calculate(){if(!origin||!destination)return;const seq=++calcSeq.current;setBusy(true);setMsg('Buscando categorias e preços...');const{data,error}=await supabase.rpc('get_passenger_ride_options',{p_origin_lat:origin.lat,p_origin_lng:origin.lng,p_destination_lat:destination.lat,p_destination_lng:destination.lng});if(seq!==calcSeq.current)return;if(error){setBusy(false);setMsg(error.message);setOptions([]);return}const rows=(data||[]) as RideOption[];setOptions(rows);const first=rows[0];if(!first){setBusy(false);setMsg('Serviço indisponível nesta área no momento.');return}const{data:ps,error:pse}=await supabase.rpc('get_effective_payment_settings',{p_city_id:first.city_id});if(seq!==calcSeq.current)return;setBusy(false);if(pse){setMsg(pse.message);return}const settings=(ps?.[0]||null) as PaySettings|null;setPaySettings(settings);if(!first.category_id){setMsg(`Ainda não há categorias ativas em ${first.city_name}/${first.state}.`);return}setSelected(first.category_id);const hasCard=methods.some(m=>m.method_type==='card');if(settings?.cash_enabled)setPayment('cash');else if(settings?.pix_enabled)setPayment('pix');else if(settings?.card_app_enabled&&hasCard)setPayment('card');else if(settings?.card_machine_enabled)setPayment('card_machine');setMsg('')}
 async function requestRide(){if(!origin||!destination||!selected)return;setBusy(true);setMsg('Procurando motoristas próximos...');const{data,error}=await supabase.rpc('create_passenger_ride',{p_origin_label:originLabel,p_origin_lat:origin.lat,p_origin_lng:origin.lng,p_destination_label:destinationLabel,p_destination_lat:destination.lat,p_destination_lng:destination.lng,p_category_id:selected,p_payment_method:payment});if(error){setBusy(false);setMsg(error.message);return}const rideId=String(data);const{data:r,error:re}=await supabase.from('rides').select(rideSelect).eq('id',rideId).maybeSingle();setBusy(false);if(re||!r){setMsg(re?.message||'Não foi possível abrir a corrida criada.');return}setCurrentRide(r as Ride);onRideCreated?.();setMsg('')}
 async function askCancel(){if(!currentRide)return;setBusy(true);const{data,error}=await supabase.rpc('preview_passenger_ride_cancellation',{p_ride_id:currentRide.id});setBusy(false);if(error){setMsg(error.message);return}setCancelPrompt({fee:Number(data?.cancellation_fee_amount||0),remainingFreeSeconds:Number(data?.remaining_free_seconds||0),requiresFee:!!data?.requires_confirmation})}
 async function confirmCancel(){if(!currentRide)return;setBusy(true);const{data,error}=await supabase.rpc('cancel_passenger_ride',{p_ride_id:currentRide.id,p_confirm_fee:true});setBusy(false);if(error){setMsg(error.message);return}if(data?.requires_confirmation){setCancelPrompt({fee:Number(data.cancellation_fee_amount||0),remainingFreeSeconds:0,requiresFee:true});return}setCancelPrompt(null);setMsg(data?.cancellation_fee_applied?`Corrida cancelada. ${money(data.cancellation_fee_amount)} será somado à sua próxima corrida.`:'Corrida cancelada sem taxa.');const{data:r}=await supabase.from('rides').select(rideSelect).eq('id',currentRide.id).maybeSingle();if(r)setCurrentRide(r as Ride);onRideCreated?.()}
 function newDestination(){setDestination(null);setDestinationLabel('');setOptions([]);setSelected('');setPaySettings(null);setCurrentRide(null);setCancelPrompt(null);setStep('search');setMsg('')}

 const validOptions=options.filter(o=>o.category_id),selectedOption=validOptions.find(o=>o.category_id===selected),savedCards=methods.filter(m=>m.method_type==='card')
 const trackDriver=!!currentRide?.driver_id&&['accepted','driver_arriving','in_progress'].includes(currentRide.status)
 const remainingSeconds=currentRide?.status==='driver_arriving'&&currentRide.driver_departed_at?Math.max(0,Math.ceil((new Date(currentRide.driver_departed_at).getTime()+Number(currentRide.cancellation_free_seconds||0)*1000-now)/1000)):0
 const allowedPayments=[...(paySettings?.pix_enabled?['pix']:[]),...(paySettings?.card_app_enabled&&savedCards.length?['card']:[]),...(paySettings?.card_machine_enabled?['card_machine']:[]),...(paySettings?.cash_enabled?['cash']:[])]

 if(currentRide&&!['completed','cancelled'].includes(currentRide.status))return <div style={{background:'#f6f7f9',borderRadius:24,overflow:'hidden',color:'#111'}}><div style={{position:'relative',height:430}}><div ref={mapEl} style={{position:'absolute',inset:0}}/><div style={{position:'absolute',top:16,left:16,right:16,display:'flex',justifyContent:'space-between',alignItems:'center',pointerEvents:'none'}}><div style={{...card,padding:'10px 14px',fontWeight:900,pointerEvents:'auto'}}>CLICK-GO</div><div style={{...card,padding:'10px 14px',fontWeight:800,pointerEvents:'auto'}}>{statusLabel(currentRide.status)}</div></div></div><div style={{background:'#fff',borderRadius:'28px 28px 0 0',marginTop:-28,position:'relative',padding:'22px 18px 26px',display:'grid',gap:14}}><div><div style={{fontSize:22,fontWeight:950}}>{statusLabel(currentRide.status)}</div><div style={{color:'#777',marginTop:4}}>{currentRide.origin_label} → {currentRide.destination_label}</div></div>{trackDriver&&<PassengerDriverTracker rideId={currentRide.id} driverId={currentRide.driver_id!}/>} {currentRide.status==='driver_arriving'&&<div style={{background:'#fff8db',borderRadius:14,padding:12,fontWeight:700}}>{remainingSeconds>0?`Cancelamento sem taxa por mais ${remainingSeconds}s`:'A taxa de cancelamento pode ser aplicada conforme a regra da categoria.'}</div>}<button onClick={askCancel} disabled={busy} style={{border:'1px solid #ddd',background:'#fff',borderRadius:15,padding:14,fontWeight:850,cursor:'pointer'}}>Cancelar corrida</button></div>{cancelPrompt&&<div style={{position:'fixed',inset:0,zIndex:9999,background:'rgba(0,0,0,.55)',display:'grid',placeItems:'center',padding:22}}><div style={{background:'#fff',color:'#111',borderRadius:24,padding:24,maxWidth:430,width:'100%',boxShadow:'0 20px 70px rgba(0,0,0,.35)'}}><div style={{fontSize:24,fontWeight:950,marginBottom:10}}>{cancelPrompt.requiresFee?'Há taxa de cancelamento':'Cancelamento sem taxa'}</div><p style={{lineHeight:1.5,color:'#555'}}>{cancelPrompt.requiresFee?`Se você cancelar agora, será registrada uma taxa de ${money(cancelPrompt.fee)}, cobrada na sua próxima corrida. Deseja realmente cancelar?`:'Você ainda pode cancelar sem taxa. Deseja cancelar esta corrida?'}</p><div style={{display:'grid',gap:9,marginTop:18}}><button onClick={()=>setCancelPrompt(null)} style={yellowBtn}>Continuar chamado</button><button onClick={confirmCancel} disabled={busy} style={{border:0,background:'#eee',borderRadius:15,padding:14,fontWeight:850,cursor:'pointer'}}>{busy?'Cancelando...':'Cancelar mesmo assim'}</button></div></div></div>}</div>

 return <div style={{background:'#f6f7f9',borderRadius:24,overflow:'hidden',color:'#111',minHeight:650}}>{step==='search'&&<div style={{padding:'20px 16px 28px',display:'grid',gap:14}}><div style={{display:'flex',alignItems:'center',justifyContent:'space-between'}}><div><div style={{fontSize:13,fontWeight:900,color:'#777'}}>CLICK-GO</div><h2 style={{margin:'3px 0 0',fontSize:28}}>Para onde vamos?</h2></div><div style={{width:48,height:48,borderRadius:'50%',background:'#111',color:'#ffd400',display:'grid',placeItems:'center',fontWeight:950}}>CG</div></div><div style={{...card,padding:14,display:'grid',gap:12}}><div style={{display:'grid',gridTemplateColumns:'14px 1fr auto',gap:10,alignItems:'center'}}><span style={{width:11,height:11,borderRadius:'50%',background:'#18c8a2'}}/><div><div style={{fontSize:12,color:'#888',fontWeight:800}}>EMBARQUE</div><div style={{fontSize:17,fontWeight:850,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{originLabel}</div></div><button onClick={()=>setEditingOrigin(v=>!v)} style={{border:0,background:'#f2f2f2',borderRadius:12,padding:'9px 11px',fontWeight:800,cursor:'pointer'}}>Alterar</button></div>{editingOrigin&&<div style={{borderTop:'1px solid #eee',paddingTop:12,display:'grid',gap:10}}><AddressSearch title="Outro endereço de origem" placeholder="Digite pelo menos 3 letras..." onSelect={chooseOrigin}/><button onClick={()=>useMyLocation(false)} style={{border:0,background:'#111',color:'#fff',borderRadius:13,padding:12,fontWeight:850,cursor:'pointer'}}>📍 Usar minha localização atual</button></div>}<div style={{borderTop:'1px solid #eee',paddingTop:12}}><AddressSearch title="Destino" placeholder="Para onde vamos?" onSelect={chooseDestination}/></div></div><div style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:9}}>{['🏠 Casa','💼 Trabalho','⭐ Favoritos'].map(x=><button key={x} style={{border:0,background:'#fff',borderRadius:16,padding:'14px 8px',fontWeight:800,boxShadow:'0 4px 18px rgba(0,0,0,.05)',cursor:'pointer'}}>{x}</button>)}</div>{msg&&<div style={{...card,color:'#7a6200',background:'#fff9d9'}}>{msg}</div>}</div>}{step==='options'&&<><div style={{position:'relative',height:390}}><div ref={mapEl} style={{position:'absolute',inset:0}}/><button onClick={newDestination} style={{position:'absolute',top:18,left:16,width:50,height:50,borderRadius:'50%',border:0,background:'#fff',fontSize:25,fontWeight:900,boxShadow:'0 4px 20px rgba(0,0,0,.18)',cursor:'pointer'}}>←</button><div style={{position:'absolute',top:18,left:82,right:16,background:'#fff',borderRadius:16,padding:'11px 13px',boxShadow:'0 4px 20px rgba(0,0,0,.14)',fontWeight:850,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{destinationLabel}</div></div><div style={{background:'#fff',borderRadius:'28px 28px 0 0',marginTop:-26,position:'relative',padding:'20px 16px 26px',display:'grid',gap:13}}><div><div style={{fontSize:22,fontWeight:950}}>Escolha sua viagem</div>{options[0]&&<div style={{color:'#777',marginTop:3}}>{Number(options[0].distance_km).toFixed(1)} km · aprox. {Math.ceil(Number(options[0].duration_min))} min · {options[0].city_name}/{options[0].state}</div>}</div>{busy&&!validOptions.length&&<div style={{padding:24,textAlign:'center',color:'#777',fontWeight:800}}>Buscando categorias disponíveis...</div>}{!busy&&!validOptions.length&&<div style={{...card,boxShadow:'none',background:'#fafafa',textAlign:'center',padding:24}}><div style={{fontSize:42}}>🚫</div><div style={{fontSize:20,fontWeight:950,marginTop:8}}>Serviço indisponível</div><div style={{color:'#777',marginTop:6}}>{msg||'Ainda não temos uma categoria disponível para esta rota.'}</div></div>}{!!validOptions.length&&<div style={{display:'grid',gap:10}}>{validOptions.map(o=><button key={o.category_id!} onClick={()=>setSelected(o.category_id!)} style={{border:selected===o.category_id?'2px solid #111':'1px solid #e8e8e8',background:selected===o.category_id?'#fffbea':'#fff',borderRadius:20,padding:'15px 14px',display:'grid',gridTemplateColumns:'66px 1fr auto',gap:12,alignItems:'center',textAlign:'left',cursor:'pointer'}}><div style={{width:62,height:62,borderRadius:17,background:'#f2f2f2',display:'grid',placeItems:'center',fontSize:34}}>{vehicleIcon(o.required_vehicle_type)}</div><div><div style={{fontSize:18,fontWeight:950}}>{o.category_name}</div><div style={{fontSize:13,color:'#777',marginTop:4}}>{o.required_vehicle_type||'Veículo'} · {Math.ceil(Number(o.duration_min))} min</div>{Number(o.multiplier||1)>1&&<div style={{fontSize:12,color:'#a36f00',marginTop:3}}>Tarifa dinâmica {Number(o.multiplier).toFixed(2)}x</div>}</div><div style={{fontSize:19,fontWeight:950}}>{money(o.fare)}</div></button>)}</div>}{!!validOptions.length&&<><div style={{display:'flex',gap:8,overflowX:'auto',paddingBottom:2}}>{allowedPayments.map(p=><button key={p} onClick={()=>setPayment(p)} style={{flex:'0 0 auto',border:payment===p?'2px solid #111':'1px solid #ddd',background:payment===p?'#fffbea':'#fff',borderRadius:14,padding:'11px 13px',fontWeight:850,cursor:'pointer'}}>{paymentLabel(p)}</button>)}</div><button onClick={requestRide} disabled={busy||!selected} style={{...yellowBtn,opacity:busy||!selected?.6:1}}>{busy?'Aguarde...':`Solicitar ${selectedOption?.category_name||'CLICK-GO'} · ${money(selectedOption?.fare)}`}</button></>}</div></>}</div>
}
