'use client'
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';import {getMyFranchise} from '@/lib/myFranchise';
export default function Page(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const c=await getMyFranchise();const {data}=await supabase.from('rides').select('id,status,origin_label,destination_label,final_fare,estimated_fare,requested_at,cities(name,state)').eq('franchise_id',c.franchiseId).order('requested_at',{ascending:false}).limit(200);setRows(data||[])})()},[]);const brl=(v:any)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0));return <><div className="topbar"><div><div className="eyebrow">Mobilidade</div><h1 className="title">Corridas</h1><p className="subtitle">Acompanhe corridas em andamento, concluídas e canceladas da sua franquia.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Corrida</th><th>Cidade</th><th>Origem → Destino</th><th>Status</th><th>Valor</th><th>Data</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td>{r.id.slice(0,8)}…</td><td>{r.cities?`${r.cities.name}/${r.cities.state}`:'—'}</td><td>{r.origin_label} → {r.destination_label}</td><td>{r.status}</td><td>{brl(r.final_fare||r.estimated_fare)}</td><td>{new Date(r.requested_at).toLocaleString('pt-BR')}</td></tr>):<tr><td colSpan={6} className="empty">Nenhuma corrida.</td></tr>}</tbody></table></div></>}
