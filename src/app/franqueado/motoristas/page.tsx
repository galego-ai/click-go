'use client'
import {useEffect,useState} from 'react'
import {supabase} from '@/lib/supabase'
import {getMyFranchise} from '@/lib/myFranchise'
export default function Page(){const [rows,setRows]=useState<any[]>([]);const [docs,setDocs]=useState<any[]>([]);const [msg,setMsg]=useState('');let fid='';async function load(){try{fid=(await getMyFranchise()).franchiseId;const d=await supabase.from('drivers').select('id,status,online,rating,city_id,profiles(full_name,phone),cities(name,state)').eq('franchise_id',fid).order('created_at',{ascending:false});if(d.error)throw d.error;setRows(d.data||[]);const ids=(d.data||[]).map((x:any)=>x.id);if(ids.length){const x=await supabase.from('driver_documents').select('*').in('driver_id',ids).order('created_at',{ascending:false});setDocs(x.data||[])}}catch(e:any){setMsg(e.message)}}useEffect(()=>{load()},[]);async function status(id:string,status:string){const {error}=await supabase.from('drivers').update({status,online:status==='blocked'?false:undefined}).eq('id',id);if(error)setMsg(error.message);else load()}async function review(id:string,status:string){const {data:{user}}=await supabase.auth.getUser();const {error}=await supabase.from('driver_documents').update({status,reviewed_by:user?.id||null,reviewed_at:new Date().toISOString()}).eq('id',id);if(error)setMsg(error.message);else load()}return <><div className="topbar"><div><div className="eyebrow">Operação</div><h1 className="title">Motoristas</h1><p className="subtitle">Aprove documentos, bloqueie motoristas e acompanhe quem está online.</p></div></div>{msg&&<p className="empty">{msg}</p>}<div className="table-wrap"><table className="table"><thead><tr><th>Motorista</th><th>Cidade</th><th>Status</th><th>Online</th><th>Ações</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td>{r.profiles?.full_name||r.id.slice(0,8)}</td><td>{r.cities?`${r.cities.name}/${r.cities.state}`:'—'}</td><td>{r.status}</td><td>{r.online?'Sim':'Não'}</td><td><div className="toolbar"><button className="button secondary" onClick={()=>status(r.id,'approved')}>Aprovar</button><button className="button danger" onClick={()=>status(r.id,'blocked')}>Bloquear</button></div></td></tr>):<tr><td colSpan={5} className="empty">Nenhum motorista.</td></tr>}</tbody></table></div><div className="section"><h2>Documentos</h2><div className="table-wrap"><table className="table"><thead><tr><th>Motorista</th><th>Documento</th><th>Status</th><th>Ações</th></tr></thead><tbody>{docs.length?docs.map(d=><tr key={d.id}><td>{d.driver_id.slice(0,8)}…</td><td>{d.document_type}</td><td>{d.status}</td><td><div className="toolbar"><button className="button secondary" onClick={()=>review(d.id,'approved')}>Aprovar</button><button className="button danger" onClick={()=>review(d.id,'rejected')}>Rejeitar</button></div></td></tr>):<tr><td colSpan={4} className="empty">Nenhum documento pendente.</td></tr>}</tbody></table></div></div></>}
