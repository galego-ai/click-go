'use client'
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';import {getMyFranchise} from '@/lib/myFranchise';
export default function Page(){const[s,setS]=useState<any>({}),[invoices,setInvoices]=useState<any[]>([]),[sub,setSub]=useState<any>(null);useEffect(()=>{(async()=>{const c=await getMyFranchise();const [p,i,fs]=await Promise.all([supabase.from('payments').select('amount,status,created_at').eq('franchise_id',c.franchiseId),supabase.from('franchise_invoices').select('*').eq('franchise_id',c.franchiseId).order('reference_month',{ascending:false}),supabase.from('franchise_subscriptions').select('*,franchise_plans(name,monthly_fee,billing_model,percentage_rate,fixed_fee_per_ride,included_rides,overage_fee_per_ride,matrix_commission_percentage)').eq('franchise_id',c.franchiseId).eq('status','active').order('starts_at',{ascending:false}).limit(1).maybeSingle()]);const pays=p.data||[];setS({paid:pays.filter((x:any)=>x.status==='paid').reduce((a:number,x:any)=>a+Number(x.amount||0),0),pending:pays.filter((x:any)=>x.status==='pending').reduce((a:number,x:any)=>a+Number(x.amount||0),0)});setInvoices(i.data||[]);setSub(fs.data)})()},[]);const brl=(v:any)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0));return <><div className="topbar"><div><div className="eyebrow">Financeiro</div><h1 className="title">Faturamento & Comissão</h1><p className="subtitle">Acompanhe faturamento, plano contratado, comissão da matriz e mensalidades.</p></div></div><div className="grid-3"><div className="card"><div className="label">Recebido</div><div className="metric">{brl(s.paid)}</div></div><div className="card"><div className="label">Pendente</div><div className="metric">{brl(s.pending)}</div></div><div className="card"><div className="label">Plano</div><div className="metric" style={{fontSize:20}}>{sub?.franchise_plans?.name||'—'}</div></div></div>{sub&&<div className="section"><div className="card"><h2>Condições comerciais</h2><p>Mensalidade: <strong>{brl(sub.custom_monthly_fee??sub.franchise_plans?.monthly_fee)}</strong> • Comissão matriz: <strong>{sub.matrix_commission_percentage??sub.franchise_plans?.matrix_commission_percentage}%</strong> • Modelo: <strong>{sub.franchise_plans?.billing_model}</strong></p></div></div>}<div className="section"><div className="table-wrap"><table className="table"><thead><tr><th>Mês</th><th>Corridas</th><th>Faturamento bruto</th><th>Mensalidade</th><th>Uso</th><th>Comissão matriz</th><th>Total devido</th><th>Status</th></tr></thead><tbody>{invoices.length?invoices.map(i=><tr key={i.id}><td>{new Date(i.reference_month+'T00:00:00').toLocaleDateString('pt-BR',{month:'2-digit',year:'numeric'})}</td><td>{i.rides_count}</td><td>{brl(i.gross_ride_value)}</td><td>{brl(i.monthly_fee)}</td><td>{brl(i.usage_fee)}</td><td>{brl(i.matrix_commission)}</td><td>{brl(i.total_due)}</td><td>{i.status}</td></tr>):<tr><td colSpan={8} className="empty">Nenhuma fatura gerada.</td></tr>}</tbody></table></div></div></>}
