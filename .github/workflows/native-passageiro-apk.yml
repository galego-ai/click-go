name: Native Passageiro APK

on:
  pull_request:
    branches: [main]
    paths:
      - 'android-native/passageiro/**'
      - '.github/workflows/native-passageiro-apk.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android platform
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Prepare preview signing key
        run: |
          base64 -d android-native/passageiro/app/preview-debug.keystore.b64 > android-native/passageiro/app/preview-debug.keystore
      - name: Apply passenger avatar and local address search
        working-directory: android-native/passageiro
        run: python3 apply_avatar_patch.py
      - name: Build native passenger APK
        working-directory: android-native/passageiro
        run: gradle --no-daemon :app:assembleDebug
      - name: Prepare artifact
        run: |
          mkdir -p apk-output
          cp android-native/passageiro/app/build/outputs/apk/debug/app-debug.apk apk-output/CLICK-GO-Passageiro-native-v0.5.apk
      - uses: actions/upload-artifact@v4
        with:
          name: CLICK-GO-Passageiro-native-v0.5
          path: apk-output/CLICK-GO-Passageiro-native-v0.5.apk
          if-no-files-found: error
